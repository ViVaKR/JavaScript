// This is a single line of code

/* Multi-line Comment
    
    Shortcuts
    (macOS) : Shift + Option + A

    code .
    code -r .

    # create a new window
    code -n

    # change the language
    code --locale=es

    # open diff editor
    code --diff <file1><file2>

    # open file a specific line and column <file:line[:character]>
    code --goto package.json:10:5

    # disable all extensions
    code --disable-extensions .

    # Errors and warnings
    Shortcut: Shift + Cmd + M

    # 동일한 단어 자동선택 수정모드 : `Cmd + Shift + L`
*/

const x = 100;
console.log(x);
console.log("Hello, World");
console.warn("Warning");
